// Life is Feudal: Your Own — config_local.cs
// This file is auto-generated by the Docker entrypoint from environment variables.
// Do NOT edit inside the container; set env vars in docker-compose.yml instead.

$DbHost     = "${DB_HOST}";
$DbPort     = "${DB_PORT}";
$DbUser     = "${DB_USER}";
$DbPassword = "${DB_PASSWORD}";
$DbDatabase = "${DB_NAME}";

// Server address & port — 0.0.0.0 binds all interfaces inside the container
$Server::GamePort   = "${GAME_PORT}";
$Server::BindAddress = "0.0.0.0";
